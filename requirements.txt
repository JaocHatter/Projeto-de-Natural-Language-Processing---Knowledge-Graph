# Dependencies are declared in pyproject.toml; this installs the package itself.
# Prefer a virtual environment:
#     python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
# The Makefile targets work without installing anything (they set PYTHONPATH=src).
-e .
